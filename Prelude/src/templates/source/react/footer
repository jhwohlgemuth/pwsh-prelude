import PropTypes from 'prop-types';

const links = {
    prelude: 'https://github.com/jhwohlgemuth/pwsh-prelude'
};
const Footer = ({name}) => <footer>
    <p>{name} was created with <span className="heart">❤</span> using <a href={links.prelude}>Prelude</a></p>
</footer>;

Footer.propTypes = {
    name: PropTypes.string
};

export default Footer;